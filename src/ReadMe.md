Error: Set up your CMake project before selecting a target.
Solution: Install cmake on macOS
"brew install cmake"

Some tips:
>reload - needed if any installation done
>target - find the name down below and select